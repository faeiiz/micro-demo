apiVersion: apps/v1
kind: Deployment
metadata:
  name: service1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service1
  template:
    metadata:
      labels:
        app: service1
    spec:
      containers:
        - name: service1
          image: service1:local
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: PEHCHAN_URL
              value: http://pehchan:8081
            - name: POKEMON_URL
              value: http://pokemon:8082
            - name: DBZ_URL
              value: http://dbz:8083
---
apiVersion: v1
kind: Service
metadata:
  name: service1
spec:
  type: ClusterIP
  selector:
    app: service1
  ports:
    - port: 8080
      targetPort: 8080
